import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        margin: 32,
    },
    emptyText: {
        fontSize: 12,
        marginTop: 16,
        textAlign: 'center',
    },
    row: {
        flexDirection: "row",
        marginTop: 16,
    },
    subtitle: {
        color: "#000",
        fontSize: 20,
        marginVertical: 24,
    }
})

export default styles;